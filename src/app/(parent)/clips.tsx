import { ClipsDashboard } from '@/components/clips-dashboard';

/** Every clip the family has kept. Shared with the driver's tab of the same name. */
export default function ParentClipsScreen() {
  return <ClipsDashboard role="parent" />;
}
