import { FamilyMap } from '@/components/family-map';

/** The driver's view of the family map — the same screen the parent sees. */
export default function ChildMapScreen() {
  return <FamilyMap />;
}
